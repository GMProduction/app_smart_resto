

import 'package:flutter/material.dart';




class BaseBloc extends ChangeNotifier {

  bool _isLoading = false;
  bool get isLoading => _isLoading;

  // GurukuBloc gurukuBloc = GurukuBloc();
  // GurukuBloc get guruku => gurukuBloc;
  //
  // TagihanBloc tagihanBloc = TagihanBloc();
  // TagihanBloc get tagihan => tagihanBloc;
  //
  // TryoutBloc tryoutBloc = TryoutBloc();
  // TryoutBloc get tryout => tryoutBloc;
  //
  // RaporBloc raporBloc = RaporBloc();
  // RaporBloc get rapor => raporBloc;
  //
  // PeringkatBloc peringkatBloc = PeringkatBloc();
  // PeringkatBloc get peringkat => peringkatBloc;
  //
  // UtilBloc utilBloc = UtilBloc();
  // UtilBloc get util => utilBloc;
}
